**cnnconvsegnet** is an utility, based on tiny-dnn, for training a CNN that
should be used for convolutional segmentation of the image,
but it also support single image segmentation as testing option
